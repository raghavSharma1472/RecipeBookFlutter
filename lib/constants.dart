import 'package:flutter/material.dart';

// list of colors that we use in our app
const custom_white = Color(0xFFF1EFF1);
const custom_grey = Color(0xFF747474);
const custom_yellow = Colors.amberAccent;
const custom_black = Color(0xFF000000);

const kDefaultPadding = 20.0;

// our default Shadow
const kDefaultShadow = BoxShadow(
  offset: Offset(0, 15),
  blurRadius: 27,
  color: Colors.black12, // Black color with 12% opacity
);
